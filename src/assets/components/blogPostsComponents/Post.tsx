import React from 'react';
import styled from "styled-components";

export const Post = () => {
    return (
        <PostStyled/>
    );
};


const PostStyled = styled.div`
    


`

const DayStyled = styled.div`

`

const TextStyled = styled.div`

`


const ReadmeStyled = styled.div`

`
